# Core: OpenAI-compatible client (works with OpenAI and OpenRouter)
openai>=1.30.0

# Optional: only needed by the legacy openaicouncil.py sentiment scoring.
# The new council.py uses rubric-based numeric scoring and does not require it.
nltk>=3.8
